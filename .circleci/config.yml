version: 2.1
jobs:
  compile:
   docker:
      - image: z4nyx/docker:latest
   steps:
      - run:
          command: |
           git config --global user.name "Shreedhan003"
           git config --global user.email "shridhan98@gmail.com"
           git config --global color.ui false
           git clone --depth=1 https://github.com/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME} -b ${CIRCLE_BRANCH} kernel
           bash kernel/.circleci/build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
